# AUDIT — 2026-04-27

End-of-iteration audit run after F21 (on-call), F22 (vCard + custom
fields), and F04++ (calendar enhancements) landed.

## Phase 1 — automated

| Check                                   | Result          | Notes |
|----------------------------------------|-----------------|-------|
| `cd backend && npm run lint`           | ✅ pass          | eslint clean |
| `cd backend && npm run build`          | ✅ pass          | tsc clean |
| `cd backend && npm run test:coverage`  | ✅ 406/406 pass  | Lines 47.1 %, Statements 47.5 %, Branches 42.5 %, Functions 45.2 % |
| `cd frontend && npm run lint`          | ✅ pass          | eslint clean |
| `cd frontend && npm test -- --coverage`| ✅ 42/42 pass    | Lines 21.3 % |
| `cd frontend && npm run build`         | ✅ pass          | CRA production build |
| Author audit                           | ✅ clean         | `git log --pretty='%an' main \| sort -u` → Luca Ostinelli only |
| AI-reference audit                     | ✅ clean         | `grep -r 'claude\|anthropic' --exclude-dir=node_modules --exclude=CLAUDE.md` returns empty |
| ROADMAP completion                     | ✅ 22/22         | F01–F22 all marked done, every box has at least one passing test |

## Phase 2 — manual sweep

| Item                                                  | Result | Notes |
|-------------------------------------------------------|--------|-------|
| Endpoint ↔ route ↔ service coherence                 | ✅      | Every router file has a matching service; every service method called by a route. |
| `console.log/error` count in backend                 | ✅      | 0 hits outside JSDoc examples (Winston only). |
| `alert(...)` count in frontend                       | ⚠️      | 4 remaining call sites (Employees, Shifts, Schedule, Settings). Tracked in `PLAN.md` (R005). |
| `@ts-ignore` in source                                | ✅      | 0 hits. |
| Demo profile end-to-end                               | ✅      | `./scripts/demo.sh up` boots Docker, runs migrations, seeds 25 users + 14 days of shifts, frontend renders the demo banner. |
| WCAG AA on Login / Dashboard / Schedule / Shifts     | ⚠️      | Manual spot check: contrast OK after dark-mode toggle, focus rings visible, ARIA labels present on interactive controls. Automated `axe` smoke not yet wired (T020). |

## Phase 3 — security

| Item                                                              | Result |
|-------------------------------------------------------------------|--------|
| All routes mount `authenticate` (verified by `routes.*.smoke.test.ts`) | ✅      |
| All `pool.execute` calls use parameter placeholders               | ✅      |
| JWT logout invalidation                                           | ❌      | B001 — server-side blacklist not implemented yet. |
| Rate limit on `/api/auth/login`                                   | ✅      | Global limiter at 100 req / 15 min, applied via `app.use(limiter)` in `src/index.ts`. Per-route tighter limit not configured (S005 follow-up). |
| CORS origin                                                       | ✅      | Configurable via `CORS_ORIGIN`; `*` only in dev mode. |
| `.env*` gitignored                                                | ✅      | Lockfiles tracked; secrets never. |
| Demo passwords used outside the seed                              | ✅      | `demo1234` only appears in `backend/scripts/seed-demo.ts`. |
| Two-factor authentication implementation (F15)                    | ✅      | TOTP secret persisted hashed; recovery codes bcrypt-hashed; RFC 4226 D vectors covered in tests. |

## Phase 4 — sign-off

The audit is **partially clean**. Open `P1` items (block sign-off only
if user wants the strict gate):

- `B001` JWT logout blacklist (P1).
- `T010` route happy-path coverage (P1, raises coverage above 70 %).

`P2` items remain in `PLAN.md` and do not block. Bumping the coverage
gate higher than the current 25 % CI gate is **deferred** until T010
+ T020 land — a higher gate now would force test debt to be paid off
before any feature work, which is the wrong trade-off when we're still
ratcheting up.

## Recap of the answers to the user's questions

| Question | Answer |
|---|---|
| Are all features implemented? | All 22 ROADMAP items have backend+tests. Frontend wiring is partial; routes & services are ready for UI work. |
| Coverage >90 %? | No: backend 47 %, frontend 21 %. Path to 90 % documented in `PLAN.md` (R007 → T010 → T020). |
| Docs complete? | `STATUS.md`, `PLAN.md`, `ROADMAP.md`, `TESTS.md`, `AUDIT-2026-04-27.md`, `README.md`, `CLAUDE.md`, `TECHNICAL.md`, `CONTRIBUTING.md`, `backend/openapi/openapi.json` are all current as of this push. |
| Coherence? | Same Pool-injection, same response envelope, single Winston logger, single author, no AI references. R001/R002/R003/R005 tracked as cosmetic refactors. |
| On-call (F21) covered? | Yes — `on_call_periods` + `on_call_assignments` tables, `OnCallService` with capacity + status promote, `/api/on-call/*` routes, 10 service tests, 9 route smoke tests, on-call events in the iCal feed with `CATEGORIES:ON-CALL`. |
| Custom fields + vCard (F22) covered? | Yes — `user_custom_fields` table, `UserDirectoryService`, `/api/directory/*` routes, vCard 4.0 round-trip parser/builder with 10 tests, 9 service tests, 7 route smoke tests. |
| iCal as live server? | Yes — feed is rebuilt from the database on every request; emits `X-PUBLISHED-TTL:PT15M`, `REFRESH-INTERVAL;VALUE=DURATION:PT15M`, `ETag`, and respects `If-None-Match` → 304. Aggregated department feed at `/api/calendar/department/:id.ics?token=…`; per-user feed lists colleagues in DESCRIPTION; on-call appears with `CATEGORIES:ON-CALL`. |
